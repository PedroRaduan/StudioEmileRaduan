"use client";

import { Save } from "lucide-react";
import { useActionState } from "react";
import { createServiceAction, updateServiceAction, type ServiceFormState } from "./actions";

type ServiceValues = { id: string; name: string; shortDescription: string | null; fullDescription: string | null; durationMinutes: number; preparationMinutes: number; cleanupMinutes: number; recommendedReturnDays: number | null; priceCents: number | null; promotionalPriceCents: number | null; depositRequired: boolean; depositType: string | null; depositValue: number | null; minAdvanceHours: number; maxAdvanceDays: number; cancellationPolicy: string | null; beforeCare: string | null; afterCare: string | null; isOnlineAvailable: boolean; isActive: boolean; displayOrder: number; calendarColor: string };
const initialState: ServiceFormState = {};
const money = (value: number | null) => value === null ? "" : (value / 100).toFixed(2).replace(".", ",");

export function ServiceForm({ service }: { service?: ServiceValues }) {
  const [state, action, isPending] = useActionState(service ? updateServiceAction : createServiceAction, initialState);
  const depositValue = service?.depositValue === null || service?.depositValue === undefined ? "" : service.depositType === "FIXED" ? money(service.depositValue) : String(service.depositValue);
  return <form action={action} className="editor-form">{service ? <input name="serviceId" type="hidden" value={service.id} /> : null}<div className="form-grid two-columns"><div className="field-group form-wide"><label htmlFor="name">Nome do serviço</label><input defaultValue={service?.name ?? ""} id="name" maxLength={140} name="name" required /></div><div className="field-group form-wide"><label htmlFor="shortDescription">Descrição curta <span>opcional</span></label><input defaultValue={service?.shortDescription ?? ""} id="shortDescription" maxLength={300} name="shortDescription" /></div><div className="field-group form-wide"><label htmlFor="fullDescription">Descrição completa <span>opcional</span></label><textarea defaultValue={service?.fullDescription ?? ""} id="fullDescription" maxLength={4000} name="fullDescription" /></div><div className="field-group"><label htmlFor="durationMinutes">Duração em minutos</label><input defaultValue={service?.durationMinutes ?? 60} id="durationMinutes" min="5" name="durationMinutes" required type="number" /></div><div className="field-group"><label htmlFor="price">Valor <span>opcional</span></label><input defaultValue={money(service?.priceCents ?? null)} id="price" inputMode="decimal" name="price" placeholder="Ex.: 120,00" /></div><div className="field-group"><label htmlFor="promotionalPrice">Valor promocional <span>opcional</span></label><input defaultValue={money(service?.promotionalPriceCents ?? null)} id="promotionalPrice" inputMode="decimal" name="promotionalPrice" /></div><div className="field-group"><label htmlFor="preparationMinutes">Preparo antes <span>min</span></label><input defaultValue={service?.preparationMinutes ?? 0} id="preparationMinutes" min="0" name="preparationMinutes" required type="number" /></div><div className="field-group"><label htmlFor="cleanupMinutes">Intervalo depois <span>min</span></label><input defaultValue={service?.cleanupMinutes ?? 0} id="cleanupMinutes" min="0" name="cleanupMinutes" required type="number" /></div><div className="field-group"><label htmlFor="recommendedReturnDays">Retorno recomendado <span>dias · opcional</span></label><input defaultValue={service?.recommendedReturnDays ?? ""} id="recommendedReturnDays" max="730" min="1" name="recommendedReturnDays" placeholder="Ex.: 20" type="number" /></div><div className="field-group"><label htmlFor="minAdvanceHours">Antecedência mínima <span>horas</span></label><input defaultValue={service?.minAdvanceHours ?? 0} id="minAdvanceHours" min="0" name="minAdvanceHours" type="number" /></div><div className="field-group"><label htmlFor="maxAdvanceDays">Máximo de dias à frente</label><input defaultValue={service?.maxAdvanceDays ?? 90} id="maxAdvanceDays" min="1" name="maxAdvanceDays" type="number" /></div><div className="field-group"><label htmlFor="color">Cor na agenda</label><input className="color-input" defaultValue={service?.calendarColor ?? "#9A5B67"} id="color" name="color" required type="color" /></div><div className="field-group"><label htmlFor="displayOrder">Ordem de exibição</label><input defaultValue={service?.displayOrder ?? 0} id="displayOrder" min="0" name="displayOrder" required type="number" /></div><label className="check-field form-wide"><input defaultChecked={service?.depositRequired ?? false} name="depositRequired" type="checkbox" /><span>Exigir sinal para este serviço</span></label><div className="field-group"><label htmlFor="depositType">Tipo de sinal</label><select defaultValue={service?.depositType ?? "FIXED"} id="depositType" name="depositType"><option value="FIXED">Valor fixo</option><option value="PERCENT">Percentual</option></select></div><div className="field-group"><label htmlFor="depositValue">Valor ou percentual</label><input defaultValue={depositValue} id="depositValue" inputMode="decimal" name="depositValue" /></div><div className="field-group form-wide"><label htmlFor="cancellationPolicy">Política de cancelamento <span>opcional</span></label><textarea defaultValue={service?.cancellationPolicy ?? ""} id="cancellationPolicy" maxLength={4000} name="cancellationPolicy" /></div><div className="field-group form-wide"><label htmlFor="beforeCare">Orientações antes <span>opcional</span></label><textarea defaultValue={service?.beforeCare ?? ""} id="beforeCare" maxLength={4000} name="beforeCare" /></div><div className="field-group form-wide"><label htmlFor="afterCare">Orientações depois <span>opcional</span></label><textarea defaultValue={service?.afterCare ?? ""} id="afterCare" maxLength={4000} name="afterCare" /></div></div><div className="form-checks"><label className="check-field"><input defaultChecked={service?.isOnlineAvailable ?? false} name="showPublicly" type="checkbox" /><span>Exibir este serviço na página pública</span></label><label className="check-field"><input defaultChecked={service?.isActive ?? true} name="isActive" type="checkbox" /><span>Serviço ativo para novos agendamentos</span></label></div>{state.error ? <p className="form-error" role="alert">{state.error}</p> : null}<button className="button button-primary" disabled={isPending} type="submit"><Save size={18} />{isPending ? "Salvando…" : service ? "Salvar alterações" : "Criar serviço"}</button></form>;
}
