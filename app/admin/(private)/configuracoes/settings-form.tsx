"use client";

import { useActionState } from "react";
import { Save } from "lucide-react";
import { saveSettingsAction, type SettingsFormState } from "./actions";

type Settings = { studioName: string; whatsapp: string | null; email: string | null; instagram: string | null; addressLine1: string | null; city: string | null; state: string | null; publicIntro: string | null; publicAbout: string | null; primaryColor: string; minNoticeHours: number; maxAdvanceDays: number; onlineBookingEnabled: boolean; bookingHoldMinutes: number; cancellationHours: number; rescheduleHours: number; maxClientReschedules: number; cancellationPolicy: string | null } | null;
const initialState: SettingsFormState = {};

export function SettingsForm({ settings }: { settings: Settings }) {
  const [state, action, isPending] = useActionState(saveSettingsAction, initialState);
  return <form action={action} className="editor-form"><div className="form-grid two-columns"><div className="field-group form-wide"><label htmlFor="studioName">Nome do studio</label><input defaultValue={settings?.studioName ?? "Emile Raduan Beauty Face"} id="studioName" maxLength={150} name="studioName" required /></div><div className="field-group"><label htmlFor="whatsapp">WhatsApp público <span>opcional</span></label><input defaultValue={settings?.whatsapp ?? ""} id="whatsapp" inputMode="tel" name="whatsapp" placeholder="Somente números com DDI" /></div><div className="field-group"><label htmlFor="email">E-mail público <span>opcional</span></label><input defaultValue={settings?.email ?? ""} id="email" name="email" type="email" /></div><div className="field-group"><label htmlFor="instagram">Instagram <span>opcional</span></label><input defaultValue={settings?.instagram ?? ""} id="instagram" name="instagram" placeholder="@seuperfil" /></div><div className="field-group"><label htmlFor="primaryColor">Cor de destaque</label><input className="color-input" defaultValue={settings?.primaryColor ?? "#9A5B67"} id="primaryColor" name="primaryColor" type="color" /></div><div className="field-group form-wide"><label htmlFor="addressLine1">Endereço <span>opcional</span></label><input defaultValue={settings?.addressLine1 ?? ""} id="addressLine1" name="addressLine1" /></div><div className="field-group"><label htmlFor="city">Cidade <span>opcional</span></label><input defaultValue={settings?.city ?? ""} id="city" name="city" /></div><div className="field-group"><label htmlFor="state">Estado <span>opcional</span></label><input defaultValue={settings?.state ?? ""} id="state" name="state" /></div><div className="field-group"><label htmlFor="minNoticeHours">Antecedência mínima <span>horas</span></label><input defaultValue={settings?.minNoticeHours ?? 0} id="minNoticeHours" min="0" name="minNoticeHours" type="number" /></div><div className="field-group"><label htmlFor="maxAdvanceDays">Máximo de dias à frente</label><input defaultValue={settings?.maxAdvanceDays ?? 90} id="maxAdvanceDays" min="1" name="maxAdvanceDays" type="number" /></div><div className="field-group form-wide"><label className="check-field form-toggle" htmlFor="onlineBookingEnabled"><input defaultChecked={settings?.onlineBookingEnabled ?? false} id="onlineBookingEnabled" name="onlineBookingEnabled" type="checkbox" />Permitir que clientes escolham horários on-line</label></div><div className="field-group"><label htmlFor="bookingHoldMinutes">Reserva temporária <span>minutos</span></label><input defaultValue={settings?.bookingHoldMinutes ?? 10} id="bookingHoldMinutes" max="30" min="5" name="bookingHoldMinutes" type="number" /></div><div className="field-group"><label htmlFor="maxClientReschedules">Solicitações de reagendamento</label><input defaultValue={settings?.maxClientReschedules ?? 1} id="maxClientReschedules" max="10" min="0" name="maxClientReschedules" type="number" /></div><div className="field-group"><label htmlFor="cancellationHours">Prazo para cancelar <span>horas antes</span></label><input defaultValue={settings?.cancellationHours ?? 24} id="cancellationHours" min="0" name="cancellationHours" type="number" /></div><div className="field-group"><label htmlFor="rescheduleHours">Prazo para reagendar <span>horas antes</span></label><input defaultValue={settings?.rescheduleHours ?? 24} id="rescheduleHours" min="0" name="rescheduleHours" type="number" /></div><div className="field-group form-wide"><label htmlFor="cancellationPolicy">Política de cancelamento</label><textarea defaultValue={settings?.cancellationPolicy ?? ""} id="cancellationPolicy" maxLength={4000} name="cancellationPolicy" placeholder="Explique prazos e consequências de forma clara." /></div><div className="field-group form-wide"><label htmlFor="publicIntro">Texto inicial do site <span>opcional</span></label><textarea defaultValue={settings?.publicIntro ?? ""} id="publicIntro" name="publicIntro" placeholder="Uma frase curta para receber quem chega ao site." /></div><div className="field-group form-wide"><label htmlFor="publicAbout">Sobre o studio <span>opcional</span></label><textarea defaultValue={settings?.publicAbout ?? ""} id="publicAbout" name="publicAbout" placeholder="Apresente o studio com suas próprias palavras." /></div></div>{state.error ? <p className="form-error" role="alert">{state.error}</p> : null}{state.success ? <p className="form-success" role="status">{state.success}</p> : null}<button className="button button-primary" disabled={isPending} type="submit"><Save size={18} />{isPending ? "Salvando…" : "Salvar configurações"}</button></form>;
}
