# Banco exclusivo de teste: nunca copie DATABASE_URL da produção.
VALIDATION_ENV=local
VALIDATION_DATABASE_URL=postgresql://agenda_validation:validation_local_only@127.0.0.1:54339/agenda_validation
VALIDATION_DB_ACK=dedicated-non-production-database
# Apenas para staging remoto: hostname exato, sem protocolo/porta.
# VALIDATION_ALLOWED_HOST=validation-db.example.net
